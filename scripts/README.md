# Scripts

This folder contains utility scripts used during development.

* `import_scryfall_sets.php`: imports MTG catalog data from Scryfall.
* `reset_catalog.sql`: clears catalog tables for re-import.

These scripts are not required to run the application.
